 #include <stdio.h>
#include <stdlib.h>

typedef struct lst{
    int x,y;
    struct lst *next;
}lst;

lst* new_lst(int x, int y){
    lst* tmp=(lst*)malloc(sizeof(lst));
    if(tmp){
        tmp->x=x;
        tmp->y=y;
        tmp->next=NULL;
    }
    return tmp;
}

lst* step(int x,int y){
    lst tmp;
    tmp=new_lst(i,j);
    tmp->next=a[i*n+j];
    return tmp;
}

int main()
{
    int n,x1,x2,y1,y2,i,j,s;
    lst* tmp;
    scanf("%d", &n);
    scanf("%d %d", &x1,&y1);
    scanf("%d %d", &x2,&y2);
    
    lst* a[n*n];
    for(i=0;i<n;i++){
        for(j=0;j<n;j++){
            s=i*n+j;
            a[s]=NULL;
            
            // if(((i==0)||(i==1))||((i==n-1)||(i==n-2)){
            //     a[i*n+j]=NULL;
            //     tmp=new_lst(i,j);
            //     tmp->next=a[i*n+j];
            //     a[i*n+j]=tmp;
            // }
            
            if((i==0)){
                if((j>1)&&(j<n-2)){
                    a[i*n+j]=step(i*n+1,j+2);
                    
                    a[i*n+j]=step(i*n+2,j+1);
                    
                    a[i*n+j]=step(i*n+1,j-2);
                    
                    a[i*n+j]=step(i*n+2,j-1);
                }
                else if((j==1)||(j==n-2)){
                    a[i*n+j]=step(i*n+1,j-1);
                    
                    a[i*n+j]=step(i*n+2,j+1);
                    if(j==1) a[i*n+j]=step(i*n+1,j+2);
                    if(j==n-2) a[i*n+j]=step(i*n+1,j-2);
                }
                else{
                    if(j==0){
                        a[i*n+j]=step(i*n+1,j+2);
                    
                        a[i*n+j]=step(i*n+2,j+1);
                    }
                    if(j==n-1){
                        a[i*n+j]=step(i*n+1,j+2);
                    
                        a[i*n+j]=step(i*n+2,j+1);
                    }
                }
                
            }
        }
    }
    return 0;
}